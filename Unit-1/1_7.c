#include <stdio.h>
int main(){
    char ch1, ch2;
    printf("Enter two input characters");
    scanf("%c %c", &ch1, &ch2);
    printf("First inputted character is %c and Second is %c\n", ch1, ch2);
    return 0;
}